# Technocon — cPanel/Apache configuration
# Routes all requests through the Node.js app (Phusion Passenger).
# Passenger is activated automatically when cPanel's Node.js App is configured.

PassengerNodejs /usr/bin/node
PassengerStartupFile server.js
PassengerAppType node

# Let Passenger handle everything
Options -MultiViews
